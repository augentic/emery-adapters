# The cap-comparison pair's serial half (RFC-96 D11): the two-target
# orders-omnia shape at cap 1 — the serial reference. Shares the
# definition home (symlinked), fixture, model config, and blind
# acceptance set with `orders-cap-four`; compare the two runs'
# coordination-cost reports under the same wall-clock budget.
kind = "workflow"
change = "orders-and-health"
wave = "deliver"
definition = "definition"
fixture = "../../../wasm/fixture"
cap = 1
blind = "../orders-cap/blind.toml"

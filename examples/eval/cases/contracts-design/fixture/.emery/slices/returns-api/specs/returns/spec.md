# returns-api Specification

## Purpose

Author the Returns service API contracts from the design document carried in this slice.

### Requirement: Contracts follow the design

The returns endpoints, request/response schemas, and shared error shape are specified in `design.md`. The contract YAML MUST match that design exactly and introduce no additional surface.

# The cap-comparison pair's concurrent half (RFC-96 D11): the
# two-target orders-omnia shape at cap 4 (the deployment default).
# Shares the definition home (symlinked), fixture, model config, and
# blind acceptance set with `orders-cap-one`; compare the two runs'
# coordination-cost reports under the same wall-clock budget.
kind = "workflow"
change = "orders-and-health"
wave = "deliver"
definition = "definition"
fixture = "../../../wasm/fixture"
cap = 4
blind = "../orders-cap/blind.toml"

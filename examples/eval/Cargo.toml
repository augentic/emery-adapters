[package]
name = "eval"
description = "Graded live eval: a public-contract client of the shipped emery binary"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish.workspace = true

# A client of the public contract only (architecture-review T6): the
# runner spawns the shipped `emery` binary and observes argv, exit
# codes, the JSON envelope, and the committed spec set. It never
# depends on engine crates or adapter internals.
[dependencies]
serde.workspace = true
serde_json.workspace = true

[lints]
workspace = true

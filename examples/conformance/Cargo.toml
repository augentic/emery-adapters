[package]
name = "conformance"
description = "Component conformance: every source adapter instantiated under the omnia runtime with a scripted model"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish.workspace = true

# Native only: the build script compiles the caller and every `sources/*`
# adapter to `wasm32-wasip2`; the harness embeds the omnia runtime.
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
anyhow.workspace = true
omnia.workspace = true
omnia-wasi-model.workspace = true
serde_json.workspace = true
tempfile.workspace = true

[dev-dependencies]
tokio.workspace = true

[lints]
workspace = true

# Install: `cargo install cargo-make`
# Help: https://sagiegurari.github.io/cargo-make/

extend = [{ path = "examples/Makefile.toml" }]

[env]
CARGO_MAKE_EXTEND_WORKSPACE_MAKEFILE = true

[config]
default_to_workspace = true
skip_core_tasks = true
skip_crate_env_info = true
skip_git_env_info = true
skip_rust_env_info = true

# -------------------------------------
# CI Checks
# -------------------------------------
[tasks.check]
dependencies = ["fmt", "lint", "test", "test-docs", "doc"]

[tasks.ci]
dependencies = ["check", "vet", "deny"]

# -------------------------------------
# Individual Actions
# -------------------------------------

# Audit
[tasks.audit]
command = "cargo"
args = ["audit"]

# Clean
[tasks.clean]
command = "cargo"
args = ["clean"]

# Deny
[tasks.deny]
command = "cargo"
args = ["deny", "--workspace", "check"]
install_crate = "cargo-deny"

# Deps
[tasks.deps]
script = '''
cargo +nightly udeps --all-targets
'''
install_crate = "cargo-udeps"

# Fmt
[tasks.fmt]
script = "cargo +nightly fmt --all"

# Lint
[tasks.lint]
command = "cargo"
args = [
  "clippy",
  "--workspace",
  "--all-targets",
  "--all-features",
  "--",
  "-D",
  "warnings",
]
install_crate = { rustup_component_name = "clippy" }

# Outdated (advisory — never part of `ci`: any upstream release would
# fail the gate for unrelated changes; run it on demand or scheduled)
[tasks.outdated]
script = '''
cargo outdated --workspace --depth 1
'''
install_crate = "cargo-outdated"

# Test
#
# `clean` is not necessary as the `RUSTFLAGS=-Dwarnings` override differs
# from the default profile, so cargo rebuilds the workspace.
[tasks.test]
command = "cargo"
args = ["nextest", "run", "--all", "--all-features", "--no-tests=pass"]
env = { RUSTFLAGS = "-Dwarnings" }

# Test Doc
[tasks.test-docs]
command = "cargo"
args = ["test", "--doc", "--all-features", "--workspace"]

# Docs
[tasks.doc]
command = "cargo"
args = ["doc", "--no-deps", "--workspace", "--all-features", "--locked"]
env = { RUSTDOCFLAGS = "-Dwarnings" }

# Miri
[tasks.miri]
script = '''
cargo +nightly miri setup
cargo +nightly miri nextest run --no-tests=pass
'''

# Vet
[tasks.vet]
script = '''
cargo vet regenerate imports
cargo vet regenerate exemptions
cargo vet regenerate unpublished
cargo vet --locked
'''
install_crate = "cargo-vet"

[tasks.release]
command = "cargo"
args = ["build", "--workspace", "--target", "wasm32-wasip2", "--release"]

# # Publish — push one built component to its exact GHCR SemVer tag as a
# # standard Wasm OCI artifact. Used by Publish Release (adapters.yaml) and
# # as a local breakout: log in first (`gh auth token | docker login ghcr.io
# # -u <github-user> --password-stdin`). Skips (exit 0) when the version
# # tag already exists — never replaces it. GHCR has no registry-native
# # tag immutability, so this probe is the compensating control; exit 0
# # keeps Publish Release and local retries idempotent after a partial run.
# [tasks.publish]
# script_runner = "bash"
# install_crate = { crate_name = "wkg", binary = "wkg", test_arg = "--version" }
# script = '''
# set -euo pipefail

# name="${1:-}"
# if [ -z "$name" ]; then
#   echo "usage: cargo make publish <adapter>" >&2
#   exit 1
# fi

# version="$(sed -n '/^\[workspace\.package\]/,/^\[/s/^version *= *"\(.*\)"/\1/p' Cargo.toml)"
# component="target/wasm32-wasip2/release/$name.wasm"
# if [ ! -f "$component" ]; then
#   echo "missing $component — build it first: cargo make release (or cargo make adapter $name for a dev build)" >&2
#   exit 1
# fi

# repository="augentic/emery-adapters/$name"
# reference="ghcr.io/$repository:$version"

# # Manifest probe: 200 means the tag is already released — skip (do not
# # replace); 401/403/404 mean no such tag is visible (first publish).
# # The probe authenticates with the operator's gh token when available
# # so a still-private package (fresh push awaiting its GHCR visibility
# # flip) is also protected; otherwise it is anonymous.
# credentials=""
# if command -v gh > /dev/null 2>&1; then
#   credentials="$(gh auth token 2> /dev/null || true)"
# fi
# token="$(curl -fsSL ${credentials:+-u "token:$credentials"} \
#   "https://ghcr.io/token?scope=repository:$repository:pull" \
#   | sed -n 's/.*"token" *: *"\([^"]*\)".*/\1/p' || true)"
# status="$(curl -s -o /dev/null -w '%{http_code}' \
#   -H "Authorization: Bearer $token" \
#   -H "Accept: application/vnd.oci.image.manifest.v1+json" \
#   "https://ghcr.io/v2/$repository/manifests/$version")"
# case "$status" in
#   200) echo "already published $reference"; exit 0 ;;
#   401 | 403 | 404) ;;
#   *) echo "unexpected registry response HTTP $status probing $reference" >&2; exit 1 ;;
# esac

# wkg oci push "$reference" "$component"
# echo "published $reference"
# '''

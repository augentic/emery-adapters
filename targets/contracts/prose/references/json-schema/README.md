# JSON Schema Sub-Flow

Index of the JSON Schema format sub-flow run by the contracts build prompt. Read exactly one file per invocation — the format sub-prompt selects the intent.

| File | Read when |
| --- | --- |
| [author.md](author.md) | Authoring or extending a JSON Schema document from a Specify change. |
| [importer.md](importer.md) | Normalising an operator-supplied external JSON Schema document onto Specify conventions. |
| [verifier.md](verifier.md) | Verifying an existing JSON Schema artefact (read-only; renders a validation report). |

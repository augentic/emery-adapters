# `vectis validate` decisions log — moved

This sidecar's content (§A–§L, the appendices, and the materialization / verify decisions) was re-homed verbatim to the repository-root [`DECISIONS.md`](../../../DECISIONS.md) at RFC-61 Step 5 Milestone A1, under §"Vectis validation and materialization". Heading text — and therefore every anchor fragment (for example `#k--materialization-and-render-by-kind`) — is unchanged there.

This stub is deleted together with the extension crate at Milestone A2.

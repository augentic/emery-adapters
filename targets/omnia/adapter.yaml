# yaml-language-server: $schema=https://github.com/augentic/specify/raw/main/schemas/target.schema.json
name: omnia
version: "1.0.0"
axis: target
description: Omnia Rust WASM target adapter. Supplies idiom guidance for core synthesis (guidance), drives crate / test / guest generation and code review (build), and gates the slice landing with cargo + wasm32-wasip2 verification (merge).

# Omnia build — standards review (moved)

The standards review is no longer a build leg: RFC-90 splits it onto the dedicated `review` operation, dispatched by the engine after verification passes. The prompt lives at [`../review.md`](../review.md) — the agent-team pipeline, finding-ID conventions, and the single-pass phase-report contract are all there. This pointer preserves inbound links (e.g. from the shared `codex/rules/universal/` README) until they migrate.

# Routing for the `contracts/design` scenario: the adapter under test, the seam
# operation `engine eval scenario` drives, and the slice name.
adapter = "target:contracts"
operation = "build"
slice = "returns-api"

# Deterministic artifact-exists gate: a success report must leave the
# slice contracts delta non-empty.
expect = [".specify/slices/returns-api/contracts"]

# Proposal — returns-api

Author API contracts for the Returns service from the design document carried in this slice (authorship mode: generate from prose, source-document path).

Participants:

- returns-service: producer
- storefront: consumer
- customer-support-console: consumer

Scope: the two returns endpoints in the design, their request/response schemas, and a shared error shape. Contract YAML only, under the slice's `contracts/` delta.

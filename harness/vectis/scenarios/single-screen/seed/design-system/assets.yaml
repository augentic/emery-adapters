# Symbol-only inventory: platform symbols need no canonical masters and
# no materialized exports, so the guest's prepare prelude reports
# `skipped: true` for this scenario.

version: 1

provenance:
  sources:
    - kind: manual
      captured_at: "2026-07-01T00:00:00Z"

assets:
  refresh:
    kind: symbol
    role: icon
    symbols:
      ios: arrow.clockwise
      android: refresh

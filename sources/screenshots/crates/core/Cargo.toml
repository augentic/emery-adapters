[package]
name = "specify-screenshots-core"
description = "Wasm-free core of the screenshots source adapter: embedded prose registry and the survey / extract judgment operations"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish.workspace = true
# Integration tests compile to the single `it` binary (one binary per
# crate); the per-area files are pulled in via tests/it.rs `#[path]`
# includes.
autotests = false

[[test]]
name = "it"
path = "tests/it.rs"

[dependencies]
specify-guest-kit.workspace = true

[build-dependencies]
specify-prose-registry.workspace = true

[dev-dependencies]
tokio.workspace = true

[lints]
workspace = true

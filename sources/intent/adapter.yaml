# yaml-language-server: $schema=https://github.com/augentic/specify/raw/main/schemas/source.schema.json
name: intent
version: "1.0.0"
axis: source
description: >-
  Operator-supplied free-form intent. Degenerate N=1 source — one lead per
  slice, one `kind: intent` claim per Evidence — that backs every plan including
  pure-greenfield work.
